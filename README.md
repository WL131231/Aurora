# UnTrack

자동매매 봇 프로젝트.

## 개요
(추후 작성)

## 환경 설정
1. 저장소 클론
   ```bash
   git clone https://github.com/WL131231/UnTrack.git
   cd UnTrack
   ```
2. `.env.example` 복사 후 본인 값 채우기
   ```bash
   cp .env.example .env
   ```
3. 의존성 설치 (언어/매니저 확정 후 추가 예정)

## 협업 규칙
- `main` 직접 커밋 금지 — 항상 feature 브랜치에서 작업
- 작업 시작 전 `git pull` 먼저
- `git push --force` 금지
- 커밋 메시지 prefix: `feat:`, `fix:`, `refactor:`
- 파일 담당 영역: (팀원 추가 후 확정)

## 라이선스
(추후 결정)
