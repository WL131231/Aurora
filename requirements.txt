# Aurora 런타임 의존성
ccxt>=4.4.0
pandas>=2.2.0,<3.0
numpy>=1.26.0,<2.0  # v0.1.77 fix R″ — numpy 1.26.x 박음 (검증된 macOS 11+ wheel)
# Why: ChoYoon Claude #133 14th 권장 fix R (`<2.3` → 2.2.x) verify 측 부족 본질
# 발견 — numpy 2.2.6 wheel 도 `macosx_14_0_arm64` only. v0.1.76 빌드 log 환기 ✅:
#   Downloading numpy-2.2.6-cp311-cp311-macosx_14_0_arm64.whl
# numpy 2.x 전반 macOS 14 wheel 박힘 (Apple Accelerate NEWLAPACK_ILP64). 2.0 미만
# (1.26.x) 박음 → 검증된 `macosx_11_0_arm64` wheel + Apple Accelerate 미사용.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pywebview>=5.0
python-telegram-bot>=21.0
python-dotenv>=1.0.0
pydantic>=2.0
pydantic-settings>=2.0
websockets>=12.0
httpx>=0.27.0

# 백테스트 데이터 수집 (PR-2)
tenacity
pyarrow

# pandas-ta 는 PyPI에서 Python 3.11 호환 버전이 사라진 상태.
# 지표는 core/indicators.py 에서 pandas/numpy 로 직접 구현 (필요 시 후속 추가).
